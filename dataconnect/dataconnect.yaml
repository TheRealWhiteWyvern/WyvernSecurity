specVersion: "v1"
serviceId: "wyvernsecurity"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wyvernsecurity-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
